pipelines:
  test_it:
    queue_strategy: replace
    # error: not allowed for queue_strategy: replace
    queue_partition_limit: 2
    concurrency: 1

    tasks:
      test:
        script:
          - echo "Foo"
